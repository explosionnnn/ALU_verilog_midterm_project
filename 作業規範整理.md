# 114-2 計算機組織 Midterm Project：ALU Design 規範整理

## 重要時程

| 事項 | 時間 |
|------|------|
| 報告上傳截止 | 2026/05/15 PM 9:00 至 i-Learning |
| 機測時間填寫截止 | 2026/05/15 PM 9:00 至電學 702 門口 |
| 機測日期 | 2026/05/18 電學 310 |
| 機測當天 | 需同時繳交報告書面資料 |

---

## 一、功能需求（7 項運算）

| 功能 | Control Signal (6-bit, Decimal) |
|------|---------------------------------|
| AND  | 36 |
| OR   | 37 |
| ADD  | 32 |
| SUB  | 34 |
| SLT  | 42 |
| SRL  | 02 |
| MULTU| 25 |

---

## 二、模組設計規範

### (1) ALU — 機測門檻模組

- 包含 AND、OR、ADD、SUB、SLT（32-bit）
- 必須從 Full Adder 做起，以 Ripple-Carry 連接 32 個 1-bit ALU Bit Slice
- 建模方式：Gate-Level Modeling + Data Flow Modeling（Continuous Assignments）
- 禁止使用 `+` operator
- 禁止使用 Always Block 或 Procedure Assignment
- 類型：組合邏輯（Combinational Logic）

### (2) Multiplier

- 32-bit 無號數乘法（MULTU）
- 採用 First Version Sequential Multiplier（參考講義[2] P.02-P.14）
- 可使用 Always Block 或 Procedure Assignment
- 禁止使用 for/while 迴圈
- 類型：循序邏輯（Sequential Logic），須以 Clock 同步

### (3) Shifter（Barrel Shifter）

- 32-bit 邏輯右移（SRL）
- 建模方式：Data Flow Modeling（Continuous Assignments）
- 禁止使用 `>>` 或 `<<` operator
- 禁止使用 Always Block 或 Procedure Assignment
- 以 160 個 Mux 實現
- 類型：組合邏輯（Combinational Logic）

### (4) HiLo 暫存器

- 64-bit 暫存器，儲存乘法器計算結果（Hi/Lo 各 32-bit）
- 類型：循序邏輯（Sequential Logic），須以 Clock 同步

### (5) Mux（多工器）

- 以 Data Flow Modeling 設計
- 類型：組合邏輯（Combinational Logic）

### (6) ALU Control

- 根據 6-bit 控制訊號決定運算類型（見上方功能表）
- 類型：循序邏輯（Sequential Logic），須以 Clock 同步

### (7) Testbench

- 以讀檔方式讀入測試資料，不可 hardcode 在 Testbench 裡
- 可包含延遲（`#constant`），用作 Clock 週期與 Reset
- 機測時助教會使用不同測試資料

---

## 三、全域禁止事項（所有模組，Testbench 除外）

| 禁止項目 | 說明 |
|---------|------|
| `for` / `while` 迴圈 | 全部模組皆不接受 |
| `Function` / `Task` 敘述 | 全部模組皆不接受 |
| `always @(*)` 敘述 | 全部模組皆不接受 |
| 延遲 `#constant` | 只有 Testbench 可用 |
| 7 項運算以外的電路 | 即使已註解，每部分扣學期總成績 5% |
| 程式碼中的註解 | 需全部移除，未移除扣分 |

---

## 四、設計注意事項

1. 一個 Module 一個檔案，以 Module 名稱命名，不可全寫在同一模組
2. Datapath 需以 PowerPoint 重新繪製，並詳述設計方法（未繪製者報告扣分）
3. Testbench 依提供之參考格式，將測試指令轉換成指定格式輸入
4. 所有設計須通過 Modelsim 模擬，使用課程介紹之 Verilog 語法
5. 須使用 AI Agent 協作，並提交完整 AI 工具清單、Prompt、對話紀錄（僅附連結扣分）
6. 機測時須能詳細解釋程式內容與設計概念（無法回答扣分）

---

## 五、報告繳交內容

1. 報告 Word 電子檔（至少 8 頁，依「計算機組織報告格式」）
2. 架構圖 PowerPoint 電子檔
3. AI 工具使用紀錄報告（Word 電子檔）
4. 所有程式檔案與執行目錄

檔名格式：
```
CO_Midterm_班級_組別_組員學號_組員姓名_重傳次數.7z
例：CO_Midterm_二甲_第三組_10227000_王大明_1.7z
```

報告必含章節：
- 組別、學號、班別、姓名
- Datapath 與詳細架構圖（PowerPoint 繪製）
- 設計重點說明
- Modelsim 驗證結果與 Waveform 輸出圖形（附說明）
- 心得感想
- 各組員分工與負責項目

---

## 六、建議完成流程

### Phase 1：理解架構
- [ ] 閱讀課程講義 [1] P.12-P.28（ALU / Barrel Shifter）
- [ ] 閱讀課程講義 [2] P.02-P.14（Sequential Multiplier）
- [ ] 閱讀講義 [3] Verilog Concepts
- [ ] 確認整體 Datapath：ALU → Mux → Output；Multiplier → Hi/Lo → Mux

### Phase 2：模組實作（建議順序）

```
Step 1: 1-bit Full Adder（Gate-Level）
Step 2: 1-bit ALU Bit Slice（AND/OR/ADD/SUB/SLT）
Step 3: 32-bit ALU（串接 32 個 Bit Slice，Ripple-Carry）
Step 4: Mux（2-to-1，Data Flow）
Step 5: Barrel Shifter（160 個 Mux 組成，Data Flow）
Step 6: HiLo 暫存器（Sequential，Clock 同步）
Step 7: Multiplier（First Version，Sequential，無迴圈）
Step 8: ALU Control（根據 6-bit signal 控制，Sequential）
Step 9: 頂層整合模組（連接所有元件）
Step 10: Testbench（讀檔方式）
```

### Phase 3：驗證

- [ ] 每個模組完成後立即用 Modelsim 模擬驗證
- [ ] 確認 ALU 七項功能全數正確（AND/OR/ADD/SUB/SLT/SRL/MULTU）
- [ ] 確認 Control Signal 對應正確（AND=36, OR=37, ADD=32, SUB=34, SLT=42, SRL=02, MULTU=25）
- [ ] 使用助教提供的測試資料跑完整 Testbench
- [ ] 擷取 Waveform 截圖備用

### Phase 4：報告製作

- [ ] 以 PowerPoint 繪製完整 Datapath 架構圖
- [ ] 撰寫設計重點說明
- [ ] 整理 AI 工具使用紀錄（工具名稱、Prompt、對話紀錄）
- [ ] 依格式撰寫報告（8 頁以上）
- [ ] 移除所有程式碼中的註解
- [ ] 確認沒有超出 7 項運算的額外電路

### Phase 5：繳交前檢查

- [ ] 所有模組通過 Modelsim 模擬
- [ ] 程式碼無任何迴圈（for/while）、Function/Task、always @(*)
- [ ] Testbench 為讀檔方式（非 hardcode）
- [ ] 報告含全部必要章節（至少 8 頁）
- [ ] AI 對話紀錄完整附上（非僅連結）
- [ ] 壓縮檔命名格式正確
- [ ] 2026/05/15 PM 9:00 前上傳 i-Learning
- [ ] 2026/05/15 PM 9:00 前至電學 702 填寫機測時間

---

## 七、評分警示

> - ALU 為機測門檻：設計不符規範或功能錯誤 → 不予評分，不得參與機測
> - 多餘電路（即使已註解）→ 每處扣學期總成績 5%
> - 抄襲（含被抄）→ 0 分
> - 機測當天無法解釋設計 → 扣分
